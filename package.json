{
  "name": "mewmoire",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "font:subset": "node scripts/build-font-subset.mjs",
    "dev": "npm run font:subset && node node_modules/@11ty/eleventy/cmd.cjs --serve",
    "build": "rm -rf _site && npm run font:subset && node node_modules/@11ty/eleventy/cmd.cjs",
    "deploy": "bash scripts/publish.sh"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0"
  }
}
